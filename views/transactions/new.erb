<form class="form" action="/transactions" method="post">
  <label>
    Select Merchant:
    <select name="merchant_id">
      <% for merchant in @merchants %>
      <option value="<%= merchant.id %>"><%=    merchant.name%></option>
      <% end %>

    </select>
  </label>
  <br>
  <label>
    Select Tag:
    <select name="tag_id">
      <% for tag in @tags %>
      <option value="<%= tag.id %>"><%= tag.type %></option>
      <% end %>
    </select>
  </label>
  <br>
  Amount: £
  <input type="number" name="amount" value="">
  <input type="submit" name="" value="Add New Transaction">
</form>
