<h1 class="edit-title">Edit Transaction</h1>
<form class="edit-form" method="post" action="/transactions/<%= @transaction.id %>" >
  <label>
    Change Merchant:
    <select name="merchant_id">
      <% for merchant in @merchants %>
      <option value="<%= merchant.id %>"><%=    merchant.name%></option>
      <% end %>

    </select>
  </label>

  <label>
    Change Tag:
    <select name="tag_id">
      <% for tag in @tags %>
      <option value="<%= tag.id %>"><%= tag.type %></option>
      <% end %>
    </select>
  </label>

  Edit Amount:
  <input type="number" name="amount" value="<%= @transaction.amount %>">
  <input type="submit" name="" value="Accept Changes Transaction">
</form>
